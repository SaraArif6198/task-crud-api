# Screenshots

Two images are referenced by the root `README.md` and need a one-time manual capture
(run `npm run dev` first):

- **swagger.png** — open http://localhost:3000/docs and screenshot the endpoint list.
- **web-ui.png** — open http://localhost:3000 in a browser and screenshot the task manager.

Save them here with those exact filenames.
